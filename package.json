{
  "name": "velarvo-desktop",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "audit": "bun audit --audit-level high && cd frontend && bun run audit",
    "build:frontend": "make fe-build",
    "check": "make check",
    "commitlint": "commitlint",
    "format": "make fmt",
    "format:check": "make format-check",
    "lint": "make lint",
    "precommit": "make precommit",
    "prepush": "make prepush",
    "test": "make test",
    "typecheck": "make typecheck-frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  },
  "overrides": {
    "fast-uri": "^3.1.3"
  }
}
